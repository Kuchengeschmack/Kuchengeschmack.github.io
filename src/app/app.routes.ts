import { Routes } from '@angular/router';
import { PhotoCardComponent } from 'app/core/components/photo-card/photo-card.component';
import { GalleryComponent } from './feature/gallery/gallery.component';

export const routes: Routes = [{ path: '', component: GalleryComponent }];
